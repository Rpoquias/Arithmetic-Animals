using UnityEngine;

public class RareAnimal : MonoBehaviour
{
    public string questionText = "What is 2 + 2?";
    public int correctAnswer = 4;

   /* private void OnMouseDown()
    {
        // Assuming the UI panel and question setup are in the PlayerInteraction script
        PlayerInteraction.Instance.DisplayQuestion(questionText, correctAnswer);
    }
   */
}